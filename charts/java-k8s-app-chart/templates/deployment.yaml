apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "java-k8s-app.fullname" . }}
  labels:
    app: {{ include "java-k8s-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "java-k8s-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "java-k8s-app.name" . }}
    spec:
      containers:
        - name: {{ include "java-k8s-app.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
